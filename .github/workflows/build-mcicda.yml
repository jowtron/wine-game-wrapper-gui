name: Build mcicda.dll

on:
  push:
    branches: [ main ]
    paths: [ 'mcicda/**' ]
  pull_request:
    branches: [ main ]
    paths: [ 'mcicda/**' ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2022

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Visual Studio Build Tools
      uses: microsoft/setup-msbuild@v2

    - name: Setup CMake
      uses: jwlawson/actions-setup-cmake@v2
      with:
        cmake-version: '3.28.x'

    - name: Configure CMake (32-bit)
      run: |
        mkdir build
        cd build
        cmake ../mcicda -G "Visual Studio 17 2022" -A Win32

    - name: Build
      run: |
        cd build
        cmake --build . --config Release

    - name: Collect artifacts
      run: |
        mkdir artifacts
        copy build\Release\mcicda.dll artifacts\
      shell: cmd

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: mcicda.dll
        path: artifacts/
        retention-days: 30
